<div class="well well-sm">
  <div>
  <h5><%= current_user.email %></h5>
  </div>
</div>

<p>
<div class="col-md-8">
  <%= form_for [@user, @item] do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, placeholder: "New List"  %>
    <%= f.submit "Save" %>
  <% end %>
    <div class="form-group" >
    </div>
  </div>
</p>

<div class="container">
  <div class="col-md-8">
    <table>
      <% @items.each do |i| %>
        <tr>
          <td><%= link_to [@item, @item.user] %></td>
        </tr>
      <% end %>
    </table>
  </div>
</div>
