<%= form_for [@item] do |f| %>
  <div class="form-group">
    <%= f.label :title %>
     <%= f.text_field :name, placeholder: "List Item" %>
  </div>
  <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>
